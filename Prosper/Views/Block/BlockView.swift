import SwiftUI

struct BlockView: View {
    var body: some View {
        NavigationStack {
            Text("Block")
                .font(.title2)
                .foregroundStyle(.secondary)
                .navigationTitle("Block")
        }
    }
}
