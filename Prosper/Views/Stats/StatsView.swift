import SwiftUI

struct StatsView: View {
    var body: some View {
        NavigationStack {
            Text("Stats")
                .font(.title2)
                .foregroundStyle(.secondary)
                .navigationTitle("Stats")
        }
    }
}
